import { InsertTextDirective } from './insert-text.directive';

describe('InsertTextDirective', () => {
  it('should create an instance', () => {
    const directive = new InsertTextDirective();
    expect(directive).toBeTruthy();
  });
});
